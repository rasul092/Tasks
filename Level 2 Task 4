#include <iostream>
using namespace std;
int main() {
    int n1;
    cout << "Bir ədəd daxil edin: ";
    cin >> n1;
    if (n1%2==0) {
        cout << "Ədəd cüt rəqəmdir." << endl;
    } else {
        cout << "Ədəd tək rəqəmdir." << endl;
    }
    return 0;
}
